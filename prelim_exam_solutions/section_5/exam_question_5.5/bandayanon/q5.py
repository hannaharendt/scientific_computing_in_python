#!/usr/bin/env python

import os, string, sys

filesystem = [sys.argv[1]]
curdir = os.listdir(os.curdir)
string


for string in os.listdir(os.curdir):
     if string.endswith(filesystem):
          print string
